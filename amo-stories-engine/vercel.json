{
    "buildCommand": "cd web && npm run build",
    "outputDirectory": "web/dist",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://amo-stories-engine-production.up.railway.app/api/:path*"
        }
    ]
}